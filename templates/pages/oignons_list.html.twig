{% extends "base.html.twig" %}

{% block title %}
	Liste des oignons - CRUDcroustillant
{% endblock %}

{% block body %}

	{% for message in app.flashes('error') %}
		<div class="alert alert-danger mt-4">
			{{ message }}
		</div>
	{% endfor %}
	<div class="container mt-4">
		<h1>Liste des oignons</h1>
	</div>
	<table class="table table-hover mt-4">
		<thead>
			<tr>
				<th scope="col">Oignon</th>
				<th scope="col">Burger associé</th>
			</tr>
		</thead>
		<tbody>

			{% for oignon in oignons2 %}
				<tr>
					<th scope="row">{{ oignon[0].name }}</th>
					<td>
						{% for burger in oignon[1] %}
							{{ burger.name }}
							{% if not loop.last %},
							{% endif %}
						{% endfor %}
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
